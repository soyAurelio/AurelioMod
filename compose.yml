# AurelioMod — Dev Environment
# Fase 1: MVP — Servidor único con Docker Compose
# Versiones pineadas — Junio 2026

services:
  # === Cache L1/L2 (BLAKE3 + pHash) ===
  dragonfly:
    image: docker.dragonflydb.io/dragonflydb/dragonfly:v1.38.1
    container_name: aureliomod-dragonfly
    ports:
      - "6380:6379"
    command:
      - "--cache_mode=true"
      - "--maxmemory=4gb"
      - "--dbnum=1"
      - "--minloglevel=2"
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 5s
      retries: 3
    volumes:
      - dragonfly_data:/data

  # === Message Queue (async Edge ↔ Engine) ===
  nats:
    image: nats:2.14.2-alpine
    container_name: aureliomod-nats
    ports:
      - "4222:4222"
      - "8222:8222"
    command:
      - "--jetstream"
      - "--store_dir=/data"
      - "-m=8222"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8222/healthz"]
      interval: 5s
      retries: 5
    volumes:
      - nats_data:/data

  # === Vector Database (L3 — búsqueda semántica) ===
  weaviate:
    image: cr.weaviate.io/semitechnologies/weaviate:1.37.7
    container_name: aureliomod-weaviate
    command:
      - "--host"
      - "0.0.0.0"
      - "--port"
      - "8080"
      - "--scheme"
      - "http"
    ports:
      - "8090:8080"
      - "50051:50051"
    environment:
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: "true"
      PERSISTENCE_DATA_PATH: "/var/lib/weaviate"
      DEFAULT_VECTORIZER_MODULE: "none"
      ENABLE_MODULES: ""
      CLUSTER_HOSTNAME: "node1"
      QUERY_DEFAULTS_LIMIT: "25"
      LOG_LEVEL: "info"
      RAFT_BOOTSTRAP_EXPECT: "1"
      CLUSTER_IN_LOCALHOST: "1"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/v1/.well-known/ready"]
      interval: 10s
      retries: 5
    volumes:
      - weaviate_data:/var/lib/weaviate

  # === WebSocket/Real-time (dashboard updates) ===
  centrifugo:
    image: centrifugo/centrifugo:v6.8.1
    container_name: aureliomod-centrifugo
    ports:
      - "8000:8000"
    command: -c /centrifugo/config.json
    volumes:
      - ./deployments/centrifugo.json:/centrifugo/config.json:ro
    ulimits:
      nofile:
        soft: 65535
        hard: 65535
    depends_on:
      dragonfly:
        condition: service_healthy

  # === Object Storage (dev) ===
  minio:
    image: minio/minio:latest
    container_name: aureliomod-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      retries: 5
    volumes:
      - minio_data:/data

  # === Observabilidad ===
  victoriametrics:
    image: victoriametrics/victoria-metrics:v1.144.0
    container_name: aureliomod-vmetrics
    ports:
      - "8428:8428"
    command:
      - "--storageDataPath=/storage"
      - "--retentionPeriod=30d"
    volumes:
      - vmetrics_data:/storage

  tempo:
    image: grafana/tempo:latest
    container_name: aureliomod-tempo
    ports:
      - "3200:3200"
      - "4317:4317"
      - "4318:4318"
    command:
      - "-config.file=/etc/tempo.yaml"
    volumes:
      - ./deployments/tempo.yaml:/etc/tempo.yaml:ro
      - tempo_data:/var/tempo

  grafana:
    image: grafana/grafana:latest
    container_name: aureliomod-grafana
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
    volumes:
      - grafana_data:/var/lib/grafana

  # === Content Analysis Engine (ConnectRPC) ===
  engine:
    build:
      context: .
      dockerfile: deployments/Dockerfile.engine
    container_name: aureliomod-engine
    ports:
      - "9090:8080"
    environment:
      WAVESPEED_API_KEY: "${WAVESPEED_API_KEY:-}"
      DRAGONFLY_ADDR: "dragonfly:6379"
      NATS_URL: "nats:4222"
      WEAVIATE_ADDR: "http://weaviate:8080"
      OTEL_EXPORTER_OTLP_ENDPOINT: "http://tempo:4317"
      OTEL_SERVICE_NAME: "engine"
      PORT: "8080"
      MEDIA_SANDBOX_ENABLED: "true"
      WEBRISK_ENABLED: "true"
      WEBRISK_API_KEY: "${WEBRISK_API_KEY:-}"
      NEON_AUDIT_ENABLED: "true"
      NEON_DATABASE_URL: "${NEON_DATABASE_URL}"
      R2_AUDIT_ENABLED: "true"
      R2_ENDPOINT: "http://minio:9000"
      R2_ACCESS_KEY_ID: "minioadmin"
      R2_SECRET_ACCESS_KEY: "minioadmin"
      R2_BUCKET: "aureliomod-audit"
      R2_REGION: "us-east-1"
    depends_on:
      dragonfly:
        condition: service_healthy
      nats:
        condition: service_healthy
      weaviate:
        condition: service_healthy
      minio:
        condition: service_healthy

  # === yt-dlp Sidecar (sandboxed URL content fetching) ===
  yt-dlp:
    image: jauderho/yt-dlp:2024.12.06
    container_name: aureliomod-ytdlp
    restart: unless-stopped
    command: >
      --no-progress
      --print-json
      --max-redirects 5
      --no-check-certificates
    healthcheck:
      test: ["CMD", "yt-dlp", "--version"]
      interval: 30s
      retries: 3
      start_period: 10s

volumes:
  dragonfly_data:
  nats_data:
  weaviate_data:
  minio_data:
  vmetrics_data:
  tempo_data:
  grafana_data:
